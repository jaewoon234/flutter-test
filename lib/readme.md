간단한 Todo 앱으로, 다음 기능을 포함합니다:

- 작업 추가 / 상태 변경 (todo / doing / done)
- 필터(전체 / todo / doing / done)
- 검색, 제목 정렬, 마감일(due date) 정렬
- 마감일 선택
- 초기 데이터 로딩(1초 지연 시뮬레이션) 및 로딩 UI
- 에러 시연(검색 옆 달력 아이콘 클릭 → 에러 UI 표시)
- 다크 모드 지원 (시스템 테마 따름)
- Riverpod 으로 상태 관리

1. 실행방법

- Flutter SDK가 설치되어 있어야 합니다.
  - 최소 요구 Dart/Flutter 범위: `Dart >= 3.0.0`, `Flutter >= 3.0.0` (프로젝트 `pubspec.yaml`에 맞춰 설정)
- 적절한 디바이스/에뮬레이터가 준비되어 있어야 합니다 (Android emulator, iOS simulator, 또는 Chrome for web).

2. 에러 시연 방법

- 앱을 실행한 뒤 검색 입력란 오른쪽의 달력 아이콘(🗓) 을 누르면 의도적으로 에러 상태가 발생합니다.

- 화면에 에러가 발생했습니다. 다시 시도해 주세요. 와 함께 다시 시도 버튼이 나오며, 버튼을 누르면 재시도(데이터 로드)가 수행됩니다.

3. 사용한 기술 및 버전 (상세)

Flutter SDK: >=3.0.0 <4.0.0

플러터 버전 : 3.38.2

라이브러리:

flutter_riverpod: ^2.3.7 — 상태관리 (StateNotifier + AsyncValue 사용)

플랫폼: Android / iOS (반응형으로 설계)

4. 과제 진행 시 집중한 부분

- UI(사용자 경험 및 반응형 레이아웃) 에 중점을 두었습니다.
- 모바일(세로)에서 자연스럽게 보이도록 1열 레이아웃 유지
- 화면 폭이 넓어지면 (태블릿/웹) 카드들을 2~3열로 배치하여 가독성 향상
- 카드 내부에 마감일, 제목, 상태 변경 드롭다운을 명확히 배치

5. 시간이 더 있었다면 (개선하고 싶었던 점)

시간이 조금더 여유로웠다면 UI를 조금더 깔끔하고 전문적으로 다듬고, 반응형 세부 튜닝, 상태관리와 아키텍처 심화, DB 연동을 했을것 같습니다.

걸린 시간은 아직 전문적 지식이 부족하기도 하고, 공부하면서 찾으면서 진행을 하여 4시간 정도 걸렸습니다. 거의 찾아보고 적용하면서 했기에 많이 부족 하기도 하고 모르는 부분도 아직 많습니다. 죄송합니다..
